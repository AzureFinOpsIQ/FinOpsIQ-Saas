# frontend

Next.js frontend application for FinOpsIQ.

Application source remains in this folder. Deployment artifacts remain colocated with the frontend service.

Frontend changes are validated through the shared CI scan workflow.
