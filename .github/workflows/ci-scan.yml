name: CI Scan

on:
  pull_request:
    branches:
      - dev
    types:
      - opened
      - reopened
      - synchronize
    paths:
      - "frontend/**"
      - "api-gateway/**"
      - "auth-service/**"
      - "collection-service/**"
      - "processing-service/**"
      - "ai-service/**"
      - "notification-service/**"
      - "services/**"
      - "shared/**"
      - "src/**"
      - "requirements*.txt"
      - "package*.json"

permissions:
  contents: read
  pull-requests: read
  security-events: write

jobs:
  ci-scan:
    name: Reusable CI Scan
    uses: AzureFinOpsIQ/FinOPsIQ-Workflows/.github/workflows/app-ci-scan.yml@63a531810cf233093c2dcd113c6d9d2f3ebd9562
    with:
      sonar_organization: ${{ vars.SONAR_ORGANIZATION }}
      sonar_project_prefix: ${{ vars.SONAR_PROJECT_PREFIX }}
    secrets:
      sonar_token: ${{ secrets.SONAR_TOKEN }}
      snyk_token: ${{ secrets.SNYK_TOKEN }}
      slack_webhook_url: ${{ secrets.SLACK_WEBHOOK_URL }}
